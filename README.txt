requirements.txt is present, use if needed. Pretrained Imagenet weigths are usually downloaded at runtime.

DataExploration_Preprocessing_Modelling_Main.ipynb contains the methods used for preprocessing.

Train_ResNET.py can be used for all ResNET models, it requires an numpy array as input. This array can be built using the notebook provided.

Submission_19 will be generated when Submission_pipeline.py is run.